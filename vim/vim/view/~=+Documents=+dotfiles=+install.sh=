let s:so_save = &so | let s:siso_save = &siso | set so=0 siso=0
argglobal
let s:l = 12 - ((1 * winheight(0) + 11) / 22)
if s:l < 1 | let s:l = 1 | endif
exe s:l
normal! zt
12
normal! 0
lcd ~/Documents/dotfiles
let &so = s:so_save | let &siso = s:siso_save
doautoall SessionLoadPost
" vim: set ft=vim :
